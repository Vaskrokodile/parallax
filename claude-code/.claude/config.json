{
  "mcpServers": {
    "parallel_agents": {
      "command": "npx",
      "args": ["-y", "@parallaxmcp/parallax-mcp"],
      "env": {
        "PARALLEL_AGENTS_STATE_DIR": ".parallel-agents"
      }
    }
  }
}
